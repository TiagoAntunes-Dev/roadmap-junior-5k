// Explicito

let x: number = 2;

//Implicito 

let y = 3; // TypeScript infers that y is a number

// Data Types

// Number

let z: number; // When you declare a variable with a type annotation, you are explicitly telling TypeScript what type the variable should be. In this case, z is explicitly declared as a number.

z = Infinity // z can be assigned any number, including special values like Infinity or NaN

// Positive Number, Negative Number, Zero and Decimal Number

let positiveNumber: number = 10; // Positive number

let negativeNumber: number = -5; // Negative number

let zero: number = 0; // Zero

let decimalNumber = 3.14; // Decimal number (TypeScript  is infers that decimalNumber is a number)