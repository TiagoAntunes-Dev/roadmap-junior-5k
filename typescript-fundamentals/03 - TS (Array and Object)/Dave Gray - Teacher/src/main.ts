let stringArr = ['One', 'Hey', 'Tiago']

let guitars = ['Strat', 'Les Paul', 5150]

let mixedData = ['EVH', 1984, true]

stringArr[0] = 'Johan'
stringArr.push('Hey')

guitars[0] = 1984
guitars.unshift('Jim')

// guitars = stringArr
// mixedData = guitars

let test = []
let bands: string[] = []
bands.push('Van Halen')

// Turple (What is)
let myTurple : [string, number, boolean] = ['Tiago', 25, true]
let mixed = ['Cruyff', 1, false]

myTurple[1] = 100

// Object
let myObj: object
myObj = []
console.log(typeof myObj)