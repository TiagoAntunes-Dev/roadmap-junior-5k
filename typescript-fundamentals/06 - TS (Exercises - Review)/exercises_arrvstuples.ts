// Você está criando o sistema de um e-commerce. O banco de dados pede que você armazene a cor de um produto usando o padrão RGB (Vermelho, Verde, Azul), que é sempre composto por exatos três números (ex: [255, 0, 128]).

// Sua Missão: 1. Crie uma variável chamada corRGB.

// 2. Não a tipe como um simples array de números (number[]), pois isso seria uma "garagem" onde cabem 10 números ou nenhum.

// 3. Tipe-a como uma Tupla para garantir que seja um "armário" com exatas 3 gavetas para números. Atribua valores a ela

const rgb : [number,number,number] = [205, 0, 122];

console.log(rgb[0]) // Output: 205
console.log(rgb[1]) // Output : 0
console.log(rgb[2]) // Output : 122